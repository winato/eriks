import styled from 'styled-components';

import { blueColor } from '../../constants/colors';

const Heading = styled.h5`
  color: ${blueColor};
  font-size: 1.2rem;
  margin-top: 0;
`

export {
  Heading,
}
